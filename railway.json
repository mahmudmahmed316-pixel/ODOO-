{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'export HOST=$PGHOST PORT=$PGPORT USER=$PGUSER PASSWORD=$PGPASSWORD; /entrypoint.sh odoo -d $PGDATABASE'"
  }
}